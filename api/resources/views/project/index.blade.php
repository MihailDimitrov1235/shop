<html>
    <head>

    </head>
    <body>
        <!-- @foreach($project in $projects)
            {{$project->name}}
        @endforeach -->
        <!-- {{$projects[0]->name}} -->
    </body>
</html>